/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost'],
  },
  assetPrefix: '',
  basePath: '',
  trailingSlash: false,
}

module.exports = nextConfig
